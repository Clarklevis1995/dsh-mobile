# Mobile 多网关接入工作计划

本次依据 Gateway 第一阶段协议及用户截图实现。Gateway 文档是协议参考，不执行其中的部署、发布或管理接口操作。保留工作区原有未提交修改。

- [x] 核对协议、连接和缓存入口
- [x] 独立主机资料、旧配置幂等迁移、凭证隔离
- [x] 配对身份校验、多地址恢复和历史主机探活
- [x] 首页主机栏、历史列表、名称和类型编辑
- [x] 独立业务状态容器、页面与文件隔离
- [x] 选择式主机管理：编辑后左侧显示 radio，普通状态只显示指示灯，铅笔从右侧滑入
- [x] 单选显示红色“删除”，多选显示红色“批量删除”；确认后批量移除资料和凭证
- [x] 自动化验证（iOS 148、共享层 138、Android 80；构建/Lint、ktlint、detekt 通过）
- [x] iOS 与 Android 模拟器核对普通、编辑未选择和编辑单选状态
- [x] 人工验收说明（multi-gateway-app-acceptance.md）

边界：一次只连接当前主机的业务通道；历史状态只在前台列表打开时有界探测。协议没有设备类型，地址推断后允许用户覆盖。真实扫码、多机和网络切换须人工验收。

- [x] Android 本地双网关设备级冒烟结果归档（A → B → A，同资源 ID 隔离及两台探活通过）
- [ ] 真实 iPhone/Android 扫码与服务器/PC 多机人工验收（需人工执行）

- [ ] 本机真实 Gateway 面板配对：已通过 Chrome 将运行模式切到“临时开启”，设备鉴权生效；iOS 模拟器中的旧资料未能自动重连，尚未生成新的一次性配对凭证。

- [x] 按界面反馈修正 iOS 语义文字颜色，移除两端主机面板的新增配对表单；双端构建通过，iOS 模拟器浅色面板实测通过。

- [x] Android 主机列表及编辑面板采用与 iOS 一致的中性色、圆角分组卡片和居中标题；移除默认紫色主题，模拟器界面检查、构建及 Lint 通过。

- [x] 两端交互终稿：移除逐行删除图标和右上角固定“完成”；进入编辑模式后显示 radio 和铅笔，未选择时右上角为空，单选显示“删除”，多选显示“批量删除”。
- [x] 两端会话页移除主机名：iOS 删除额外安全区主机栏，Android 标题改为单行并将标题栏收紧至 54dp；分段栏上下间距统一由 12 缩至 6，为对话列表释放纵向空间。
- [x] 首页主机选择栏与主标题间距收紧：iOS 由 72pt 调整为 44pt，Android 由 54dp 调整为 44dp；双端构建、Lint 与模拟器画面核对通过。
- [x] 主机列表使用蓝色“当前主机”状态文字区分当前连接目标；其他主机继续使用次要文字色，在线灯语义不变。
- [x] 收紧主机列表项上下留白：iOS 行内上下间距由 6pt 调为 2pt；Android 最小行高由 76dp 调为 68dp、上下间距由 16dp 调为 10dp。
- [x] 收紧首页品牌标识与主机选择栏间距：两端从页面默认 18pt/dp 调整为独立的 6pt/dp。
- [x] 修复 Android 手动认证面板取消无响应：历史主机重连时断开当前连接，新配对时取消配对任务；取消立即退出连接态，底层清理使用 2 秒超时兜底。
- [x] 按 iOS 视觉参数细化 Android 主机组件：顶部主机图标和纵向留白缩小，面板操作文字增大，列表图标缩小、两行文字间距收紧，并降低当前主机蓝色亮度。
- [x] 修复 Android 新建会话响应竞争：`session-created` 由唯一 Runtime 事件消费者分发给发起方；本机完成选中、订阅并跳转，模拟器连接真实 Gateway 点击验收通过。
- [x] 修复 Android 已完成回复缺少复制按钮：只隐藏当前轮仍在生成的回复操作，保留流式 ID 的历史终帧正常显示复制按钮；模拟器点击后显示蓝色完成勾。
- [x] 对齐 Android 主机列表层级：增大“我的主机”分组标题，并为相邻主机增加从文字列开始的细分割线；普通态和编辑态模拟器核对通过。
- [x] Android 在线状态增加轻微绿色光晕：覆盖首页主机栏、工作区卡片、主机列表、会话页及运行中会话圆点；“已连接”增加更弱的文字光感，离线与异常状态保持无光晕。
- [x] 移除 iOS 与 Android 主机编辑行右侧的排序拖拽条及其占位；radio、在线灯和铅笔交互保持不变。
- [x] 降低 Android 主机面板顶部操作按钮的视觉深度：浅色模式改用低透明度白色蒙层，深色模式保留弱对比，尺寸与点击范围不变。
- [x] iOS 与 Android 主机列表始终将当前主机置顶，其余主机保持原有相对顺序；只调整展示顺序，不改写持久化资料。
